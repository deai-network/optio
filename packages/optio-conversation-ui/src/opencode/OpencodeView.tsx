import type { WidgetProps } from 'optio-ui';

// Placeholder: replaced wholesale by the real opencode protocol view in the
// opencode adapter task.
export function OpencodeView(_props: WidgetProps) {
  return <div>opencode protocol view</div>;
}
