# Handoff (2026-09-13): state not recorded elsewhere

Written by session optio-bb (commissura `aoe:885eb398ad53`) before a context
reset; updated by its successor optio-6b (same AoE id) after the snapshot fix
was deployed. Hosts: `superego` (workstation), `excavator` (dev stack; optio and
excavator checked out in `~/deai/`), `plaplay.lan` (the "Access from" worker).

## Done

1. **Snapshot throughput fix** (design:
   `docs/2026-09-13-snapshot-archive-throughput-design.md`): 256 KiB block reads,
   `pigz -1` (gzip -1 fallback) under `bash -c 'set -o pipefail; ...'`, remote
   stderr tail in errors, channel closed when a capture is abandoned, missing
   exit status treated as failure. Commits 0b65aae3, 68f66517, 82a01c0b; two
   review rounds; deployed by fast-forwarding `excavator:~/deai/optio` main
   (engine restarted 08:21). **Verified live 2026-09-13 08:23**: owner resumed
   and cancelled analysis `6aa42dea`; capture 13.0 s in total, archive 320 MB
   in 12.1 s (26.5 MiB/s), "Snapshot saved", state `cancelled`.

## Open work, in order

1. **Analysis process `6aa42dea1b791783f5a01e83`** (plakomm, claudecode on
   plaplay): state `cancelled`, latest snapshot `6aa6416f5a82951c79a64862`
   (session blob `6aa641635a82951c79a64352`, workdir blob
   `6aa641635a82951c79a6435b`). Stops no longer fail. Older snapshots kept:
   `6aa634a2…` (manual restore of the 06:58 stop), `6aa614cc…`, `6aa4d6af…`,
   `6aa4b779…`. The manual restore procedure
   (`docs/2026-09-13-manual-snapshot-restore.md`) should no longer be needed.
2. **The owner disabled the automatic sync tasks** (2026-09-13 ~06:45) after
   plaplay went offline; re-enabling is the owner's call.
3. **Unpushed local mains** (owner: push only after manual testing): optio
   `82a01c0b` (on origin `4aebde18`; unwind diagnostic logging, docs, snapshot
   fix), excavator `fa4a0e54` (on origin `30942a62`; engine log file, hook
   timing, env docs).
4. **Releases pending** (owner approval): patch wave per
   `docs/release-cookbook.md` — optio-agents 0.6.1 (prompt composer, `fs_isolation_dirs`,
   unwind tracing), the seven wrappers with `optio-agents>=0.6.1,<0.7`
   (optio-claudecode 0.6.3 also carries the resume-notice change `6aa2d269`),
   optio-host patch (tracing + snapshot throughput fix), no optio-agents-all
   release. **Excavator main must not be pushed before that optio-host release
   exists and `packages/engine/pyproject.toml` requires it**: excavator's
   default cancel grace is now 30 s (was 180 s), which only holds with the fast
   archive, and the engine image installs optio-host from PyPI (0.3.0 today).

## Deployed diagnostics

- `OPTIO_CANCEL_TRACE=1` in `excavator:~/deai/excavator/.env.local` (loaded before
  optio imports); `run_command` is traced only during an unwind.
- Engine log: `excavator:~/.excavator/logs/engine.log` (10 MiB × 5,
  `EXCAVATOR_ENGINE_LOG_FILE`, empty disables). `engine.log.test-junk-*` next to
  it is test output from before the import-time fix; delete it.
- Process-page milestones during a capture: `Stopping: agent stopped`,
  `Snapshot: session state saved`, `Snapshot: archiving workdir, N MB so far`,
  `Snapshot: workdir archived`, `Snapshot saved`. The engine log's
  `archive_workdir START` line names workdir and compressor.

## Deferred review findings (not fixed)

- Prompt composer branch: `render_resume_section` is public; codex and grok
  `host_actions.py` `build_resume_notice_args` docstrings still name
  `_SYSTEM_PREFIX_EXPLAINER`; wrapper `compose_agents_md` docstrings document only
  `workdir_exclude`; guide line 338 is 139 chars; no fresh-start session-level
  docs-threading tests; per-wrapper duplicate "no prompt text" checks (by design).
- Unwind logging: optio-agents has no direct test that `unwind_tracing()` is set
  on its finally path; tasks created before the unwind block escape the
  ContextVar (none call `run_command` today); three swallow-with-RAISED blocks in
  claudecode keep hand-rolled timing (could use `_traced` with the try outside);
  `test_configure_logging_unwritable_path_does_not_raise` doesn't assert the
  console warning; "agent stopped" has integer-second precision.
- Snapshot fix (`RemoteHost.archive_workdir`): if the connection drops while
  the generator is paused at `yield`, the stderr reader task can end with an
  unretrieved exception (asyncio log noise only); no trace line when a capture
  is abandoned or fails with an exception (only DONE is logged); the stderr
  tail in the error can start mid-line, and a signal kill shows as `exit -1`
  rather than naming the signal. Later options from the design doc: overlap
  SSH reads with GridFS writes, zstd, excluding regenerable caches.
- Postponed by the owner: dedupe of the six `_append_resume_log_entry` and five
  `build_resume_notice_args` copies (fragile; needs line-by-line reading).

## Known issues (not yet fixed)

- A failed stop still leaks `tail -F optio.log` and the SFTP session on the
  agent host, and the engine keeps that SSH connection open. (The archive
  pipeline itself is now closed when a capture is abandoned.)
- `excavator` has no `unzip` (opencode/kimicode installs fail for tasks without
  an "Access from" host; 5 install tests fail) and no `pigz` (its own tasks
  would archive with gzip -1). pigz and bash are not checked by `make install`.
- optio-claudecode tests sharing `process_id="p"` collide under xdist; SSH-
  container tests (`test_remote_deliverable_callback_fired`,
  `test_remote_run_command_captures_stdout`, and optio-host's
  `test_remote_host_stdin.py` fixture) flake under load, also on main.
- Bring-up gotchas guide (host prerequisites, config, open items; now lists
  pigz and bash): `superego:~/deai/stack-bringup-gotchas-2026-09-12.md` (not in
  a repo).

## Leftovers to clean when done

- `excavator:~/excavator-forensics/`: scripts (`restore_snapshot.py`,
  `restore2_snapshot.py`, `extract_session.py`, `unwind-deploy.sh`), transcript
  copy `claude_session_6aa5dfa5.jsonl`, restore tarballs (×2, ~300 MB each),
  cancel-capture logs, older restore records.
- `plaplay:~/excavator-forensics-plaplay/`: two workdir backups (~740 MB each),
  two restore tarballs, `analysis-workdir-20260912T032952`.
- `superego:/tmp/claude_session.log` (transcript copy for the owner),
  `superego:/tmp/unwind/` (ledger, reports, review packages),
  `superego:/tmp/optio-archive-throughput/` (working copies of the fix).
- GridFS session blobs no snapshot references: `6aa49b369b676d727d010b19`,
  `6aa4a5419b676d727d010b34`. (`6aa5dfa5335595b3998b33a0` was listed here
  before, but snapshot `6aa614cc…` references it: keep it.)
