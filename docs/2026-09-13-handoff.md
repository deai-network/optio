# Handoff (2026-09-13): state not recorded elsewhere

Written by session optio-bb (commissura `aoe:885eb398ad53`) before a context
reset. Hosts: `superego` (workstation), `excavator` (dev stack; optio and
excavator checked out in `~/deai/`), `plaplay.lan` (the "Access from" worker).

## Open work, in order

1. **Snapshot throughput fix**: design in
   `docs/2026-09-13-snapshot-archive-throughput-design.md`. Implemented on
   branch `archive-throughput` (worktree `excavator:~/deai/optio-archive-throughput`)
   by session optio-6b. The owner
   chose pigz -1 plus 256 KiB block reads. Deploy only via branch + worktree +
   review, then fast-forward `~/deai/optio` while nothing runs (engine restart).
2. **Analysis process `6aa42dea1b791783f5a01e83`** (plakomm, claudecode on
   plaplay): state `failed`. Its latest snapshot is the manual restore
   `6aa634a28bf2fdaf05e0ff89` (workdir blob `6aa6349c8bf2fdaf05e0fa82`, session
   blob `6aa62d7b9cd67b963fcc2e93`) = the state at the 2026-09-13 06:58 stop.
   Earlier fallbacks: `6aa614cc…` (restore of the 23:26Z stop) and `6aa4d6af…`.
   **Until the fix is deployed, any stop of this task fails again** (~280 s
   capture > 180 s grace); the restore procedure is in
   `docs/2026-09-13-manual-snapshot-restore.md`.
3. **The owner disabled the automatic sync tasks** (2026-09-13 ~06:45) after
   plaplay went offline; re-enabling is the owner's call.
4. **Unpushed local mains** (owner: push only after manual testing): optio
   `64c6a381` (on origin `4aebde18`; unwind diagnostic logging), excavator
   `fa4a0e54` (on origin `30942a62`; engine log file, hook timing, env docs).
5. **Releases pending** (owner approval): patch wave per
   `docs/release-cookbook.md` — optio-agents 0.6.1 (prompt composer, `fs_isolation_dirs`,
   unwind tracing), the seven wrappers with `optio-agents>=0.6.1,<0.7`
   (optio-claudecode 0.6.3 also carries the resume-notice change `6aa2d269`),
   optio-host patch (tracing), no optio-agents-all release.

## Deployed diagnostics

- `OPTIO_CANCEL_TRACE=1` in `excavator:~/deai/excavator/.env.local` (loaded before
  optio imports); `run_command` is traced only during an unwind.
- Engine log: `excavator:~/.excavator/logs/engine.log` (10 MiB × 5,
  `EXCAVATOR_ENGINE_LOG_FILE`, empty disables). `engine.log.test-junk-*` next to
  it is test output from before the import-time fix; delete it.
- Process-page milestones during a capture: `Stopping: agent stopped`,
  `Snapshot: session state saved`, `Snapshot: archiving workdir, N MB so far`,
  `Snapshot: workdir archived`, `Snapshot saved`.

## Deferred review findings (not fixed)

- Prompt composer branch: `render_resume_section` is public; codex and grok
  `host_actions.py` `build_resume_notice_args` docstrings still name
  `_SYSTEM_PREFIX_EXPLAINER`; wrapper `compose_agents_md` docstrings document only
  `workdir_exclude`; guide line 338 is 139 chars; no fresh-start session-level
  docs-threading tests; per-wrapper duplicate "no prompt text" checks (by design).
- Unwind logging: optio-agents has no direct test that `unwind_tracing()` is set
  on its finally path; tasks created before the unwind block escape the
  ContextVar (none call `run_command` today); three swallow-with-RAISED blocks in
  claudecode keep hand-rolled timing (could use `_traced` with the try outside);
  `test_configure_logging_unwritable_path_does_not_raise` doesn't assert the
  console warning; "agent stopped" has integer-second precision.
- Postponed by the owner: dedupe of the six `_append_resume_log_entry` and five
  `build_resume_notice_args` copies (fragile; needs line-by-line reading).

## Known issues (not yet fixed)

- A failed stop leaks the remote `tar`/`gzip`, `tail -F optio.log` and SFTP
  session on the agent host, and the engine keeps that SSH connection open.
- `excavator` has no `unzip` (opencode/kimicode installs fail for tasks without
  an "Access from" host; 5 install tests fail). pigz is not checked by
  `make install`.
- optio-claudecode tests sharing `process_id="p"` collide under xdist; SSH-
  container tests (`test_remote_deliverable_callback_fired`,
  `test_remote_run_command_captures_stdout`) flake under load, also on main.
- Bring-up gotchas guide (host prerequisites, config, open items):
  `superego:~/deai/stack-bringup-gotchas-2026-09-12.md` (not in a repo).

## Leftovers to clean when done

- `excavator:~/excavator-forensics/`: scripts (`restore_snapshot.py`,
  `restore2_snapshot.py`, `extract_session.py`, `unwind-deploy.sh`), transcript
  copy `claude_session_6aa5dfa5.jsonl`, restore tarballs (×2, ~300 MB each),
  cancel-capture logs, older restore records.
- `plaplay:~/excavator-forensics-plaplay/`: two workdir backups (~740 MB each),
  two restore tarballs, `analysis-workdir-20260912T032952`.
- `superego:/tmp/claude_session.log` (transcript copy for the owner),
  `superego:/tmp/unwind/` (ledger, reports, review packages).
- GridFS session blobs no snapshot references: `6aa49b369b676d727d010b19`,
  `6aa4a5419b676d727d010b34`, `6aa5dfa5335595b3998b33a0` (superseded).
